---
import '../styles/global.css';
import Navigation from '../components/Navigation.astro';

interface Props {
  title: string;
  description: string;
  keywords?: string;
  ogImage?: string;
  canonicalUrl?: string;
}

const {
  title,
  description,
  keywords = "web design romania, seo saloane frumusete, marketing digital clinici medicale, website salon, site clinica, bucuresti",
  ogImage = "https://digitalpro.ro/assets/images/og-image.jpg",
  canonicalUrl = "https://digitalpro.ro"
} = Astro.props;
---

<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>{title}</title>
    <meta name="description" content={description}>
    <meta name="keywords" content={keywords}>
    <meta name="author" content="Digital Pro">
    <meta name="robots" content="index, follow">
    <meta property="og:title" content={title}>
    <meta property="og:description" content={description}>
    <meta property="og:type" content="website">
    <meta property="og:url" content={canonicalUrl}>
    <meta property="og:image" content={ogImage}>
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content={title}>
    <meta name="twitter:description" content={description}>
    <link rel="canonical" href={canonicalUrl}>
    <meta name="geo.region" content="RO">
    <meta name="geo.placename" content="București, România">

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`}>
    <link rel="shortcut icon" href={`${import.meta.env.BASE_URL}favicon.svg`}>
    <link rel="apple-touch-icon" href={`${import.meta.env.BASE_URL}favicon.svg`}>

    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <!-- Flowbite Pro (Local) - after Tailwind -->
    <link rel="stylesheet" href={`${import.meta.env.BASE_URL}css/flowbite.css`}>

    <!-- Custom CSS - loaded last to override -->
    <link rel="stylesheet" href={`${import.meta.env.BASE_URL}css/custom.css`}>
</head>
<body class="bg-gray-50">

    <!-- Sticky Header Container -->
    <div class="header-container">
        <!-- Urgent Offer Bar -->
        <div class="offer-bar relative bg-gradient-to-r from-red-600 via-pink-600 to-purple-600 text-white py-3 px-4 text-center overflow-hidden">
            <div class="sparkle absolute inset-0"></div>
            <div class="relative z-10">
                <div class="font-bold text-sm md:text-base">
                    <span class="inline">🔥 OFERTĂ: 50% REDUCERE!</span>
                    <span class="hidden md:inline"> la primul website + SEO GRATUIT!</span>
                    <span class="animate-pulse inline ml-2">⏰ 48 ore!</span>
                </div>
            </div>
        </div>

        <Navigation />
    </div>

    <!-- Main Content -->
    <slot />

    <!-- Footer -->
    <footer class="bg-gray-900 text-white py-12">
        <div class="max-w-screen-xl px-4 mx-auto">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
                <!-- NAP (Name, Address, Phone) -->
                <div>
                    <h3 class="text-xl font-bold mb-4 text-gradient">Digital Pro</h3>
                    <div class="space-y-3 text-gray-300">
                        <p class="flex items-start">
                            <i class="fas fa-map-marker-alt mr-3 mt-1 text-pink-500"></i>
                            <span>Str. Exemplu Nr. 123<br>București, 010101<br>România</span>
                        </p>
                        <p class="flex items-center">
                            <i class="fas fa-phone mr-3 text-pink-500"></i>
                            <a href="tel:+40721234567" class="hover:text-pink-400">+40 721 234 567</a>
                        </p>
                        <p class="flex items-center">
                            <i class="fas fa-envelope mr-3 text-pink-500"></i>
                            <a href="mailto:contact@digitalpro.ro" class="hover:text-pink-400">contact@digitalpro.ro</a>
                        </p>
                        <p class="flex items-center">
                            <i class="fas fa-clock mr-3 text-pink-500"></i>
                            <span>Luni - Vineri: 09:00 - 18:00</span>
                        </p>
                    </div>
                </div>

                <!-- Google Map -->
                <div>
                    <h3 class="text-lg font-bold mb-4">Locația Noastră</h3>
                    <div class="w-full h-48 bg-gray-800 rounded-lg overflow-hidden">
                        <iframe
                            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2848.8444419446443!2d26.102498615674984!3d44.43679280821638!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x40b1ff476f99771f%3A0xd02c8b4d5a853e02!2sBucure%C8%99ti!5e0!3m2!1sro!2sro!4v1234567890123"
                            width="100%"
                            height="100%"
                            style="border:0;"
                            allowfullscreen=""
                            loading="lazy"
                            referrerpolicy="no-referrer-when-downgrade">
                        </iframe>
                    </div>
                </div>

                <!-- Legal & Social -->
                <div>
                    <h3 class="text-lg font-bold mb-4">Informații Legale</h3>
                    <div class="space-y-2 text-gray-300 text-sm mb-6">
                        <p><strong>CUI:</strong> RO12345678</p>
                        <p><strong>Reg. Com.:</strong> J40/1234/2020</p>
                        <p><strong>Formă juridică:</strong> S.R.L.</p>
                        <p class="pt-2">
                            <i class="fas fa-file-contract mr-2 text-pink-500"></i>
                            Contract + Factură
                        </p>
                    </div>
                    <div class="flex space-x-4 mb-4">
                        <a href="#" class="text-white hover:text-pink-400 transition-colors">
                            <i class="fab fa-facebook text-2xl"></i>
                        </a>
                        <a href="#" class="text-white hover:text-pink-400 transition-colors">
                            <i class="fab fa-instagram text-2xl"></i>
                        </a>
                        <a href="#" class="text-white hover:text-pink-400 transition-colors">
                            <i class="fab fa-linkedin text-2xl"></i>
                        </a>
                    </div>
                </div>
            </div>

            <!-- Bottom Footer -->
            <div class="border-t border-gray-800 pt-6">
                <div class="flex flex-col md:flex-row justify-between items-center text-sm text-gray-400">
                    <p>&copy; 2024 Digital Pro. Toate drepturile rezervate.</p>
                    <div class="flex flex-wrap gap-4 mt-4 md:mt-0">
                        <a href="#" class="hover:text-pink-400 transition-colors">Politica de confidențialitate</a>
                        <a href="#" class="hover:text-pink-400 transition-colors">Termeni și condiții</a>
                        <a href="#" class="hover:text-pink-400 transition-colors">Cookies</a>
                        <a href="https://anpc.ro" target="_blank" rel="noopener" class="hover:text-pink-400 transition-colors">ANPC</a>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Sticky Mobile CTA Bar (visible only on mobile) -->
    <div class="fixed bottom-0 left-0 right-0 bg-gradient-to-r from-purple-600 to-pink-600 text-white py-3 px-4 shadow-2xl z-50 md:hidden">
        <div class="flex items-center justify-around gap-2">
            <a href="tel:+40721234567" class="flex flex-col items-center justify-center flex-1 hover:opacity-80 transition-opacity">
                <i class="fas fa-phone text-xl mb-1"></i>
                <span class="text-xs font-semibold">Sună</span>
            </a>
            <a href="https://wa.me/40721234567" target="_blank" class="flex flex-col items-center justify-center flex-1 hover:opacity-80 transition-opacity">
                <i class="fab fa-whatsapp text-xl mb-1"></i>
                <span class="text-xs font-semibold">WhatsApp</span>
            </a>
            <a href="#contact" class="flex flex-col items-center justify-center flex-1 hover:opacity-80 transition-opacity">
                <i class="fas fa-calendar-check text-xl mb-1"></i>
                <span class="text-xs font-semibold">Programare</span>
            </a>
        </div>
    </div>

    <!-- Cookie Policy Bar -->
    <div id="cookie-bar" class="fixed bottom-16 md:bottom-0 left-0 right-0 bg-gray-900 text-white py-3 px-4 shadow-2xl z-50 transition-transform duration-300 transform translate-y-0">
        <div class="max-w-screen-xl mx-auto">
            <div class="flex items-center justify-between gap-4">
                <div class="flex items-center gap-3 flex-1">
                    <i class="fas fa-cookie-bite text-xl text-yellow-400 flex-shrink-0"></i>
                    <p class="text-sm">
                        Folosim cookie-uri pentru a îmbunătăți experiența ta.
                        <a href="#" class="text-yellow-400 hover:underline">Mai multe detalii</a>
                    </p>
                </div>
                <div class="flex gap-2 flex-shrink-0">
                    <button id="accept-cookies" class="bg-yellow-400 text-gray-900 px-4 py-1.5 rounded-md text-sm font-semibold hover:bg-yellow-500 transition-colors whitespace-nowrap">
                        Accept toate
                    </button>
                    <button id="reject-cookies" class="bg-gray-700 text-white px-4 py-1.5 rounded-md text-sm font-semibold hover:bg-gray-600 transition-colors whitespace-nowrap">
                        Esențiale
                    </button>
                    <button onclick="document.getElementById('cookie-bar').style.display='none'" class="text-gray-400 hover:text-white transition-colors ml-1">
                        <i class="fas fa-times text-lg"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Hidden dummy elements for Flowbite compatibility -->
    <div id="themeToggleDarkIcon" class="hidden" style="display: none !important;"></div>
    <div id="themeToggleLightIcon" class="hidden" style="display: none !important;"></div>
    <button id="themeToggle" style="display: none !important;"></button>

    <!-- Flowbite Pro JS (Local) with error handling -->
    <script is:inline>
        // Suppress Flowbite errors for missing elements
        window.addEventListener('error', function(e) {
            if (e.message && (
                e.message.includes('null is not an object') ||
                e.message.includes('Cannot read property') ||
                e.message.includes('navbar')
            )) {
                e.preventDefault();
                return true;
            }
        }, true);
    </script>
    <script is:inline src={`${import.meta.env.BASE_URL}js/flowbite.js`}></script>

    <!-- Custom App JS -->
    <script is:inline src={`${import.meta.env.BASE_URL}js/app.js`}></script>
</body>
</html>
